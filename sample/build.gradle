apply plugin: 'com.android.application'

android {
  compileSdkVersion 23
  buildToolsVersion '23.0.3'

  defaultConfig {
    applicationId 'com.example.behavior.drawer'
    minSdkVersion 7
    targetSdkVersion 23
    versionCode 1
    versionName '1.0'
  }
}

dependencies {
  compile project(':drawer-behavior')
}
